// 1.
 const pin = 123456;
let limit = 2000;
// 2. 
function checkUser (checkPin) {
    return  checkPin=== pin 
}
// 3.
 let account = 20.5490495;
function checkBalance(checkPin) {
    if (checkPin=== pin) {
        return ` Your balance is ${account.toFixed(2)} shekels`;
    }
    else {
        return "Try again";
    } 
}
// 4.
 function withdraw(amount) {
    if (amount >limit || amount > account )[
        return `The amount crossed maximum limit ${limit}.`;
    ]
}
    else if (account -= amount) {
        return `You withdrawed ${amount}, now on your account left ${account} shekels.`;

}
5. 
6. function atm(checkPin,) {
   if (checkPin === pin) {
    
   }
}

    
